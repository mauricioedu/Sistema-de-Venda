%h1 Produtos Vendidos

%table.table.table-hover
  %thead
    %tr
      %th ID
      %th Produtos
      %th Total
      %th Comprador
      %th Data
      %th

  - @orders.each do |order|
    %tr
      %td= order.id
      %td
        %ul
          -order.order_items.each do |item|
            %li
              =item.quantity
              =item.product.nome
      %td= number_to_currency_br order.total_preco
      %td
        %address
          %strong= order.user.nome
          %br
          =simple_format order.user.full_endereco
      %td
        %p=time_ago_in_words order.created_at
        %p.text-muted=order.created_at.strftime "%b %d, %Y - %H:%M"
      %td
        =order.state
